# Phase 5: Optional layers (stub)

Opt-in power-user layers: Claude hooks, GitHub private backup, paths.json `platforms.{wsl}` block.

**Status: implemented in Task 5 of the v1 roadmap.**

Each branch is independently optable; user can pick any subset.
